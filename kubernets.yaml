apiVersion: apps/v1
kind: Deployment
metadata:
  name: flask-deploy
  namespace: siva-k8s
  labels:
    app: python-app
spec:
  replicas: 2
  selector:
    matchLabels:
      app: frontend
  template:
    metadata:
      labels:
        app: frontend
    spec:
      containers:
      - name: c23
        image: us-central1-docker.pkg.dev/winter-monolith-477705-m8/resp/python22:v22
        ports:
        - containerPort: 5000
